import React from 'react';
import logo from '../../logo.svg' 

const LoadingSpinner = () => (
    <div>
        <img src={logo} className="App-logo" height="240" width="240" alt="Loading..." />
    </div>
);

export default LoadingSpinner;